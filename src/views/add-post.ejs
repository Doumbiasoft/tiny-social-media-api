<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/assets/styles/styles.css">
    <title>Tiny Social Media - Add New Post</title>
</head>

<body class="bg-gradient-to-br from-gray-50 to-blue-50 min-h-screen">
    <!-- Navigation -->
    <nav class="bg-[#1a507c] shadow-lg border-b border-gray-200">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <div class="flex items-center">
                    <h1 class="text-2xl font-bold text-white">Tiny Social</h1>
                </div>
                <div class="flex space-x-4">
                    <a href="/api/v1/views/posts"
                        class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-black cursor-pointer bg-[#f9c14f] hover:bg-[#f58831] transition-all duration-200 transform hover:scale-105 shadow-md hover:shadow-lg">
                        <- View All Posts </a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="max-w-2xl mx-auto py-12 px-4 sm:px-6 lg:px-8">
        <div class="bg-white rounded-lg shadow-xl overflow-hidden">
            <!-- Header -->
            <div class="bg-[#1a507c] px-6 py-8">
                <h2 class="text-3xl font-bold text-white">Create New Post</h2>
                <p class="text-indigo-100 mt-2">Share your thoughts with the community</p>
            </div>

            <!-- Form -->
            <form action="/api/v1/views/posts" method="POST" class="p-6 space-y-6">
                <!-- User Selection -->
                <div class="space-y-2">
                    <label for="userId" class="block text-sm font-medium text-gray-700">
                        Select User
                    </label>
                    <div class="relative">
                        <select name="userId" id="userId" required
                            class="block cursor-pointer w-full px-3 py-3 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-[#1a507c] focus:border-[#1a507c] transition-colors duration-200">
                            <option value="">-- Choose a user --</option>
                            <% users.forEach((user)=>{ %>
                                <option value="<%= user.id %>">
                                    <%= user.name %> (<%= user.email %>)
                                </option>
                                <% }); %>
                        </select>
                    </div>
                </div>

                <!-- Title Input -->
                <div class="space-y-2">
                    <label for="title" class="block text-sm font-medium text-gray-700">
                        Post Title
                    </label>
                    <input type="text" name="title" id="title" required placeholder="Enter an engaging title..."
                        class="block w-full px-3 py-3 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-[#1a507c] focus:border-[#1a507c] transition-colors duration-200">
                </div>

                <!-- Content Textarea -->
                <div class="space-y-2">
                    <label for="content" class="block text-sm font-medium text-gray-700">
                        Content
                    </label>
                    <textarea name="content" id="content" rows="6" required
                        placeholder="What's on your mind? Share your thoughts, experiences, or ideas..."
                        class="block w-full px-3 py-3 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-[#1a507c] focus:border-[#1a507c] transition-colors duration-200 resize-vertical"></textarea>
                </div>

                <!-- Submit Button -->
                <div class="pt-4">
                    <button type="submit"
                        class="w-full  flex justify-center items-center px-6 py-3 border border-transparent text-base font-medium rounded-md text-black cursor-pointer bg-[#f9c14f] hover:bg-[#f58831] transition-all duration-200 transform hover:scale-105 shadow-lg hover:shadow-xl">
                        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                        </svg>
                        Publish Post
                    </button>
                </div>
            </form>
        </div>


    </div>
    <!-- Footer -->
    <footer class="bg-white border-t border-gray-200 mt-16">
        <div class="max-w-7xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
            <div class="text-center">
                <p class="text-gray-500 text-sm">
                    © 2025 Tiny Social Media.
                </p>
            </div>
        </div>
    </footer>
</body>

</html>